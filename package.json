{
  "name": "medialab-agent",
  "version": "1.0.0",
  "description": "MediaLab LATAM — Agente Editorial IA",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node --watch backend/server.js",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
